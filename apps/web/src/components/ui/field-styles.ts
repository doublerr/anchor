/**
 * Shared form-field class strings so every input/label across the app matches
 * the standardized token palette (see globals.css). Extracted from the signup
 * form so the onboarding wizard and future forms stay visually identical.
 */
export const fieldClass =
  "rounded-md border border-border bg-background px-3 py-2 text-sm text-foreground outline-none placeholder:text-muted-foreground focus:border-gold-400 focus:ring-2 focus:ring-gold-400/30";

export const labelClass =
  "flex flex-col gap-1 text-sm font-medium text-foreground";
